-r requirements.txt
# CTFd 3.7.6 ships no setup.py/pyproject.toml, so it cannot be pip-installed
# as a package (a `CTFd @ git+...` line here would fail to build). The dev
# environment instead clones CTFd's source to .ctfd-src/ (gitignored) and
# installs its pinned requirements.txt directly, wiring it onto sys.path via
# a ctfd.pth file. See .dev/setup.sh, which does this for you.
pytest>=8.0
